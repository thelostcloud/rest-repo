name: Test Workflow

on:
  workflow_dispatch:

permissions:
  contents: read
  deployments: read
  id-token: none

jobs:
  test:
    name: Test Workflow
    runs-on: ubuntu-latest
    environment: testtest
    steps:
      - name: Hello
        run: |
          echo "Hello"
